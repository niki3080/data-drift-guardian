global:
  scrape_interval: 5s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  - job_name: drift-mock-exporter
    static_configs:
      - targets:
          - drift-mock-exporter:8000